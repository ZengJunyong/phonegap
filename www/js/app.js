// Generated by CoffeeScript 1.6.3
angular.module('myApp', ['ngRoute', 'myApp.contacts', 'myApp.events', 'myApp.battery']).config([
  '$routeProvider', function($routeProvider) {
    return $routeProvider.otherwise({
      redirectTo: '/contacts'
    });
  }
]);

document.addEventListener('deviceready', function() {
  console.log('deviceready');
  console.log(navigator.camera);
  console.log(device);
  navigator.notification.beep(1);
  navigator.geolocation.getCurrentPosition(function(pos) {
    return console.log(pos);
  });
  navigator.globalization.getPreferredLanguage(function(language) {
    return console.log(language);
  });
  document.addEventListener('offline', function() {
    return navigator.notification.alert('offline');
  }, false);
  document.addEventListener('online', function() {
    return navigator.notification.alert('online');
  }, false);
  setInterval(function() {
    return navigator.notification.alert(navigator.connection.type);
  }, 30000);
  return angular.bootstrap(document, ['myApp']);
}, false);
